#![allow(dead_code)]
mod types;
use crate::types::message::MessageContent;
use crate::types::message::MessageRequest;
use crate::types::message::DataResponse;

fn main() {
    println!("Hello, world!");
}
